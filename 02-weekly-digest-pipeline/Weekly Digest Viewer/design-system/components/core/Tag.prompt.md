Pill for a digest `theme` string. Tint by the topic the article belongs to.

```jsx
<Tag tone="finance">interest rates</Tag>
```

Themes come from `article.themes`; render them lowercase, exactly as the digest supplies them.

No brand assets were provided.

The attached codebase (`weekly-digest-viewer/`) contains only `digest-data.js`, an unstyled test
`index.html`, and a README — no logo, icons, illustrations, or imagery. Nothing was invented to fill
the gap: the wordmark is the brand name set in Literata (see `../guidelines/wordmark.html`), and
icons come from Lucide via CDN (see the ICONOGRAPHY section of `../readme.md`).

Drop real assets here and I'll wire them in.

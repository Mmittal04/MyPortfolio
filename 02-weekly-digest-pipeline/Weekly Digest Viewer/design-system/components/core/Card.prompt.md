Container for a discrete unit of digest content.

```jsx
<Card interactive accent="var(--topic-finance)">…</Card>
```

The `accent` edge is the one place a coloured left border is allowed — it encodes topic, not decoration.

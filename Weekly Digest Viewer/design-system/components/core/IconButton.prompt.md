Icon-only button for toolbar-level affordances (refresh, external link, copy).

```jsx
<IconButton label="Refresh digests" variant="outline"><i data-lucide="rotate-cw"></i></IconButton>
```

Sizes 28/36/44px. Always pass `label`.

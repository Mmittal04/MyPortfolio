Shown when a digest fetched fine but had zero articles.

```jsx
<EmptyState detail="The pipeline ran on August 25 and found nothing new." />
```

Dashed border, sand surface, no red — an empty week is not a failure.
